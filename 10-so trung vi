
#include<stdio.h>


int main() {
    int a, b, c;
    printf("nhap 3 so :");
    scanf("%d%d%d", &a, &b, &c);
    if(a > b && a > c && b >= c || c > b && c > a && b >= a || a == b && b > c) { printf("so trung vi la %d", b);
    
    } else if(b > c && b > a && a >= c || c > a && c > b && a >= b)  { printf("so trung vi la %d",a);
    
    } else if(a > b && a > c && c >= b || b> a && b > c && c >= a)   { printf("so trung vi la %d ",c);
    } else if( a == b && a == c) { printf("Ba so bang nhau"); 
    
     
       } 
		
			}
